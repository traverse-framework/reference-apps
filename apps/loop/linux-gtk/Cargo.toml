[package]
name = "loop-gtk"
version = "0.1.0"
edition = "2021"
description = "Native GTK4 client for loop (embedded Traverse runtime)"
license = "MIT"

[lib]
path = "src/lib.rs"

[[bin]]
name = "loop-gtk"
path = "src/main.rs"

[dependencies]
loop-core-rs = { path = "../loop-core-rs" }
gtk4 = "0.9"
libadwaita = { version = "0.7", features = ["v1_5"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
directories = "5"

[dev-dependencies]
