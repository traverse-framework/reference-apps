# Vendored from traverse-framework/Traverse packages/web/TraverseEmbedder
# Includes embedder-api/1.0.0 + embedded-trace-api/1.0.0 companion (spec 517 / Traverse #802).
# Refresh: cd $TRAVERSE_REPO/packages/web/TraverseEmbedder && npm i && npm run build \
#   && cp -a package.json README.md dist <App-Refs>/vendor/traverse-embedder-web/ \
#   && restore this ORIGIN.md
