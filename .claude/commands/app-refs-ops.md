---
name: "app-refs-ops"
description: "Start or resume the App-References operating model when the user says APP REFS OPS. Runs continuously until no Ready work remains or a hard blocker stops progress."
---

Read `.agents/skills/app-refs-ops/SKILL.md` and follow it exactly — it is the single canonical copy of the App-References ops loop (this file was previously a duplicate and is now a pointer, per the org token-discipline rules in `traverse-framework/.github` `docs/ai-agent-hardening.md`).
